name: build
on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths-ignore:
      - "**/README.md"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Set variables
        run: |
          echo "RELEASE_NAME=Released on $(date +%Y%m%d%H%M)" >> $GITHUB_ENV
          echo "TAG_NAME=$(date +%Y%m%d%H%M)" >> $GITHUB_ENV

      - name: Checkout custom builder
        uses: actions/checkout@v4
        with:
          repository: runetfreedom/domain-list-custom
          path: custom

      - name: Checkout domain-list-community
        uses: actions/checkout@v4
        with:
          repository: v2fly/domain-list-community
          path: community

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: ./custom/go.mod
        
      - name: Build ad block
        run: |
          curl -sSL https://adguardteam.github.io/AdGuardSDNSFilter/Filters/filter.txt | perl -ne '/^\|\|([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})\^$/ && print "$1\n"' | perl -ne 'print if not /^[0-9]{1,3}(\.[0-9]{1,3}){3}$/' > temp-reject.txt
          curl -sSL https://pgl.yoyo.org/adservers/serverlist.php?hostformat=hosts&showintro=1&mimetype=plaintext | perl -ne '/^127\.0\.0\.1\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> temp-reject.txt
          sed -i '/t.co/d' temp-reject.txt
          cat temp-reject.txt | sort --ignore-case -u > ./community/data/category-ads-all

      - name: Get and prepare lists
        run: |
          # Скачиваем списки
          curl -sSL https://antifilter.download/list/domains.lst > ./antifilter-download
          curl -sSL https://community.antifilter.download/list/domains.lst > ./community/data/antifilter-download-community
          curl -sSL https://raw.githubusercontent.com/1andrevich/Re-filter-lists/refs/heads/main/domains_all.lst > ./community/data/refilter
          
          # Жесткая очистка от любого мусора (оставляем только латиницу, цифры, точки и дефисы)
          find ./community/data/ -type f -exec sed -i '/^[a-zA-Z0-9.-]*$/!d' {} +
          
          # Создаем файл ru-blocked
          echo "include:antifilter-download-community" > ./community/data/ru-blocked
          echo "include:refilter" >> ./community/data/ru-blocked
          
          echo "include:antifilter-download" > ./community/data/ru-blocked-all
          echo "include:antifilter-download-community" >> ./community/data/ru-blocked-all
          echo "include:refilter" >> ./community/data/ru-blocked-all      

      - name: Build geosite.dat
        run: |
          cd custom
          go run ./ --exportlists=ru-blocked,youtube,discord,telegram,category-ads-all --datname=geosite.dat --datapath=../community/data

      - name: Prepare publish
        run: |
          mkdir -p publish
          install -Dp ./custom/publish/geosite.dat ./publish/geosite.dat
          (cd ./publish && sha256sum geosite.dat > geosite.dat.sha256sum)

      - name: Release and upload
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          release_name: ${{ env.RELEASE_NAME }}
          tag: ${{ env.TAG_NAME }}
          file_glob: true
          file: ./publish/*
          overwrite: true
          make_latest: true

      - name: Push to release branch
        run: |
          cd publish
          git init
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b release
          git add .
          git commit -m "${{ env.RELEASE_NAME }}"
          git remote add origin "https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}"
          git push -f -u origin release
